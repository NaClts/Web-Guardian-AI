services:
  envoy:
    build: ./envoy
    ports:
      - "80:80"
      - "443:443"
    environment:
      WEB_APP_HOST: www.example.com
      WEB_APP_PORT: 80
    depends_on:
      - auth-service-py

  auth-service-py:
    build: ./auth-service-py
